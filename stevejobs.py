def SJ():
    import time; from functions import clear
    while True:
        clear()
        print("It's so sad that Steve Jobs died of ligma.")
        response = input("> ")
        if response.lower() == "who the hell is steve jobs?":
            print("Ligma balls.")
            time.sleep(0.5)
            break
        elif response == "secret escape command":
            break
        else:
            continue